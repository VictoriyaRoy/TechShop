import FeedbackSlider from "./feedback_slider";

const Hits = () => {
    return (
        <section className="feedback">
            <h3 className="section_header">
                Out customers say
            </h3>
            <FeedbackSlider/>
        </section>
    )
}

export default Hits
